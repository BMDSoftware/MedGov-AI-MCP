Gabriel


## Research:

When downloading test data from Cancer Imaging Archive you'll also have to download the *NBIA Data Retriever* in order to download *TCIA Images*

**Data types:
- MR
- CT
- SEG
- RTSTRUCT
- DX
- NM
- PT
- SC
- CR
- RTDOSE
- Demographic
- Diagnosis
- Treatment
- Follow-up
- Other

**Format**
- NIFTI
- TSV


Cancer Imaging Archive MCP? Since they have an API we could integrate with it

The NBIA tool was bugged so I used idc-index

- Theres a slight problem because I just found out .dcm can be a single slice or they can be multislice which is less common


Right now we have more of an application style thing but thats not what we want, only...

Instead of 
inference_jobs_ session_id, file_id, model_name, status, result
We will have
backgroundt_tasks: sessions id, task type, descrip+tion, input data, status , result, created at, completed at

The reason for this is for the agent to decide what input data should be ran in background, he himself will decide what takes time or not, and this way we have a tab that gives us the results together with a notification. This was we can have other tasks on background like maybe searchingg for any information while still talking to the model.
We will have a reports and result tab.
Results / Activity


TODO: Change the hardcoded queue task tool to the mcp utils
TODO: Report no endpoint? MCP RADLEX

RadLex has find_templates, get_template_schema, and generate_report. The report flow should be:
1. Find a matching RadLex template based on modality/body part from the inference results
2. Fill it via generate_report(template_id, findings)
3. Augment with LLM-generated Impression/Recommendations

Handling the queue tasks without MCP tools

- Model now accepts directories of files to run the inferences on
- Model now accepts more than one file and sticks with it

Backend (orchestrator/)                                                                                                                                                                   
- database.py — replaced inference_jobs with a generic background_tasks table + CRUD (create_task, update_task, get_task, list_tasks)
- task_runner.py (new) — thread pool that runs any task type in the background; inference handler opens a fresh MCP session to MONAI; report handler calls RadLex MCP for template + LLM for narrative sections
- backend.py — added GET /api/events (SSE), GET /api/tasks, POST /api/generate-report; wires task_runner to the main event loop on startup

Agent (agenticAgent.py)
- Added queue_task as a built-in tool (registered alongside MCP tools so the LLM sees it)
- Agent intercepts queue_task calls before they hit MCP — calls task_runner.submit_task() and returns immediately
- System prompt updated with background task rules (kept skills workflow intact)

Frontend (frontend/src/)
- Toast.jsx + Toast.css — global SSE listener, shows toasts for task started/done/failed, auto-dismiss at 5s
- Results.jsx + Results.css — lists all background tasks with status badges, filterable, expandable rows showing structures/volumes for inference or narrative for reports
- Report.jsx + Report.css — selects completed inference results, queues report generation, renders RadReport-style output with quantitative findings + LLM sections
- App.jsx — added Results and Report nav items, mounted <Toast> globally, passes refreshSignal from SSE events down to Results and Report


Keep all files directories uploaded, session management
**Idea**
Then deal with just upload the files and everything will be taken care of. No more speaking required Even if some files are left on hold, etc, different files can be uploaded together, etc...


Changed monai mcp server
postprocess_segmentation now accepts voxel_spacing_mm: Optional[tuple] = None.
run_inference detects the input type before calling postprocess_segmentation

Corrected shutdown and suspended process

The uploading different files and keeping them, and directory was working. But I had two bugs when it came to the async inference:
  1. confirm_tool_execution doesn't forward session_id to the resumed execute_task, so it defaults to None → "unknown" which fails the FK
  2. self.logger.error() is the custom Logger class that doesn't accept exc_info=True 



Had to add the dir to the database as well because of the persistency in between sessions

- /api/set-directory now saves the directory as a file_type=dicom dir row in uploaded_files, replacing any previous one. size is also computed
- load_session now queries for dicom_dir entries and restores uploaded_dir global, so the agent gets the directory on the next query.
- process_query skips dicom_dir rows when reading session files as binary (directories can't be open()'d as files . the global uploaded_dir handles them separately)
- Frontend onLoadSession restores uploadedDir state (the green folder chip) when loading a saved session, and clears it on new session.

Keep running the event? Even when closing backend?

Need to correct keeping files and directories

Notification wasnt working well, im gonna add something to show that its running inferences or tasks in the backgruond
I also gave the model the ability to list whats currently being done
But I didnt use MCP for that


The graceful shutdown takes 3 seconds now


So its working but I need to correct a few things:

```
Starting autonomous task: analyze these two images and run inferences on them
Image path for workflow: /home/gabriel/Desktop/AgenticHealthMCP/orchestrator/data/uploads/a4e730393e36_581cc760-22fa-4fc4-8256-ca7dee98a979.dcm
Iteration 1/15
Using response from send_function_response (Gemini optimization)
[task_runner] Starting 45884383 (inference)
[task_runner] Queued inference task 45884383: Pancreas and tumor segmentation on CT scan
Processing request of type CallToolRequest
Running inference on cuda...
Loading model from /home/gabriel/Desktop/AgenticHealthMCP/mcp-monai/bundles/pancreas_ct_dints_segmentation...
Failed to parse JSONRPC message from server
Traceback (most recent call last):
  File "/home/gabriel/Desktop/AgenticHealthMCP/orchestrator/venv/lib/python3.12/site-packages/mcp/client/stdio/__init__.py", line 155, in stdout_reader
    message = types.JSONRPCMessage.model_validate_json(line)
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/gabriel/Desktop/AgenticHealthMCP/orchestrator/venv/lib/python3.12/site-packages/pydantic/main.py", line 766, in model_validate_json
    return cls.__pydantic_validator__.validate_json(
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
pydantic_core._pydantic_core.ValidationError: 1 validation error for JSONRPCMessage
  Invalid JSON: trailing characters at line 1 column 5 [type=json_invalid, input_value='2026-02-19 15:35:40.0807...to be a multiple of 32.', input_type=str]
    For further information visit https://errors.pydantic.dev/2.12/v/json_invalid
```
- Being able to upload more than one file and directory or showing at least all the uploaded currently
- Optimizations can be done regarding calling two times listing models for the two files when you can call once and determine for boht
- Notification when running inference and after finishing is not being generated (shouuld be a frontend notification like a popup)
- Test report
- Task runners should close or end after being ran?