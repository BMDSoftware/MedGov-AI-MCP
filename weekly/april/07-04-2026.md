Gabriel Santos


- Fixing the deployment problems pointed out by Martinho


1. Deploy pipeline - I think the problem is because of the .env?
2. Directory behavior
3. Cellpose v4, lets stay on that
4. Overlay outlines on original image - instead of just the mask, show the segmentation outlines drawn on the original image. save_masks tool can do this, agent can call it.
5. Blocking issue - most critical: uploading ROI makes the container unhealthy, /api/process-query blocks, async is broken somewhere.


# Done

Problem: Every time the agent calls Gemini/Ollama, it freezes the entire server (no healthchecks, no other requests) because the call is synchronous and blocks the only thread FastAPI runs on.                         
Fix: Wrap the call in run_in_executor so it runs in a background thread, freeing the event loop to handle other requests while waiting for the LLM response.                                
To test: Grepping health while waiting for message response - "docker compose logs orchestrator | grep health"
Just verify if you get healthchecks while still waiting for an answer



Model type is ignored in cellpose v4 since it only uses cpsam, so I altered the dockerfiles as well

Our gemini model is hitting quota so I'll have to deply to test.

generate the overlay inside the background task handler in task_runner.py, right after segment_cells_2d returns. The bakground task has both the original image path and the masks, it calls save_overlay directly within the same MCP session, and the result stored in the DB already has the overlay as output_path. No extra agent turn needed, it just happens automatically.              
The save_overlay MCP tool also being registered means the agent can call it interactively if a user ever asks manually


Done:
Overlay outlines:                 
- mcp-cellpose/server.py - new save_overlay tool: loads original image + masks, draws red outlines using cellpose.utils.outlines_list, saves as PNG                                        
- orchestrator/task_runner.py - _async_run_cellpose now calls save_overlay after segmentation; output_path in the result becomes the overlay PNG (visible in Results tab), raw mask path is kept as mask_path              
                                                                                                                                                                                            
Directory handling simplified:  
- orchestrator/agent/execution.py - non-DICOM directories now expand their files directly into the files list instead of showing an [IMAGE DIR] prompt asking the user to clarify           
- orchestrator/agent/prompts.py - removed the [IMAGE DIR] rule  

1. Blocking issue fixed
2. Deploy pipeline fixed
3. Cellpose v4
4. Overlay outlines done
5. Directory handling simplified (now we just expand into files available)


Treated the outline for the segmentation and made a flipper toggle for the 3 images, so I could better distinguish from the normal image, from the mask and then from the segmentation
Model was getting stuck answering to only after the task was done running in background... Not allowing to run two tasks or to queue more tasks because we are waiting for answer.
Removed 2 extra LLM iterations, maybe this was always a problem, but we had a better model running

We have to be careful what model we're using, 2.5 flash tried counting the cells himself
Altered prompt from main orchestrator for cellpose

My models arent working right now

closing/reopening EventSource doesn't happen when authToken doesn't change. The fix is to add reconnection logic in Toast.jsx on onerror


- Since cpsam is the onyl model in use now we dont need the describe nor list models
Commenting out the tool wasn't helping ebcause gemini was from its own knowledge choosing cyto3 so should I put it with cpsam hardcoded?

Task runner timeout added so gpu crashes dont leave processes hanging

TODO:

- Need to test on server because some tasks are now maybe desynced, kinda, it first queues then messages, which is normal, but the delay is much higher now, and I don't know if it's because of the model I'm using but it's very slow answering sometimes

- Should I hardcode cpsam from line 60 problem? 