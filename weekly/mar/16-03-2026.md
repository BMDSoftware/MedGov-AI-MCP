Gabriel



# Keep testing

- Removed MCP server reappears after Docker restart (he said he'd fix later)
- Model doesn't realize it changed MCP tools after adding/removing

# Ideas

- Directory analyzer, im still developing and finding out what to do
I want prompts to be able to be associated to directories, extra prompts
I want the directory to be analyzed as soon as some file is put there
For that we would need new tools and better context for full background stuff
A part for results
Maybe a frontend with several boxes corresponding to each directory where u enter it and there u have the results
And an ongoing console with results
The AI should maybe have the ability to create files and directories within to save results and stuff
Enable disable toggle
A path and a name

The flow could be: Identify what it is -> parse the file (dicom for now?) -> AI reads custom prompts -> runs what it needs to run, id like it to be other things other than inferences, even for example if it checks the file shouldnt be there it could put it somewhere else, move commands options for that -> results relative to each directory could appear in results but it could also be overwhelmed, so maybe in the boxes for each directory it would have different interfaces specific for each one
There might not be results, just notes added or something...
For example, imagine the directory has several dicom files, they all build an image, it should know they are all together and doesnt need to analyze each one, needs to find consensus, etc, dunno how to do this yet

Autonomous inbox -> real filesystem paths, that would be relative to each computer
I searched whatchdog python library
Custom prompt would be free text user types
Dedicated tab, and results for directories and console with files being analyzed and showing what the computer is running in the background relative to each console and directory as if they were processes, and directories can communicate with each other and send files over to each other, etc, this would be best case scenario


We can start simple, AI knows file shouldnt be there, creates a directory like "uncertain" and puts it there
We ignore the results tab for now, and that becomes another pipeline
New MCP utils tools, create file, create directory, move file (directory withing directory or moving to other being watched directories)
This pipeline is triggered simply by having a directory being watched, it isnt a trigger, but a passive and active background process
The console should print out everything thats happening and being executed within a directory even if its just pulls or health checks

Add ability to create files in the MCP utils, files that are exportable like PDFs, CSVs for documetns

# In progress

**Directory analyzer**
- orchestrator/watcher_service
- alter database (watched_directories table)
- new endpoints BACKEND 
- new server.py tools in mcp utils
- New sse events for the consoles and etc (file detected, debounce firing, AI action, tool call, health check)

I personalized the prompts


Altered frontend in directories tab
Altered the selection of directory, typing it out or just selecting the folder

# Done


# TODO
