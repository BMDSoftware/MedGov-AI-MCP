Gabriel

Ainda nao vi o teu PR, mas cuidado com os merges, testa primeiro
Nao sei se tens a mesma estrutura que eu de files  DICOM/ST000000/ ... etc eu estou a tentar arranjar uma forma de conseguir ler estes ficheiros
Como ja tinha explicado antes, os MR0000XX sao parte das slices 2D dos ficheiros DICOM entao os SE0000X corresponde a uma imagem 3D com as respetivas slices


Structure:
- ST000000 = Study (one patient scan session)
- SE000000 = Series 0 (5 slices) - probably a localizer/scout
- SE000001 = Series 1 (27 slices) - an MRI sequence
- SE000002 = Series 2 (32 slices) - another MRI sequence
- thumbs/ = PNG thumbnail previews (generated by a DICOM viewer, not useful)
- dicom/ = duplicate reference folder with a UID-named subfolder (viewer metadata)
- ctkDICOM.sql / ctkDICOMTagCache.sql = database files from 3D Slicer or CTK viewer


Instead of hardcoding this, ima make it in a way where you upload the directory, so its scalable also
- I wanted to make it so it also verifies the dirwectory to not upload unnecessary or bad data

What we have:
- utils.parse_dicom - Parses a single .dcm file, extracts all tags, pixel info, validates it's real DICOM.
- utils.parse_dicom_directory - goes throug a directory, parses every file, groups them by SeriesInstanceUID, reports modality, body part, slice count per series.
- monai.analyze_image - Uses MONAI's LoadImage which already handles directories of DICOM slices natively (MONAI auto-stacks them into a 3D volume).

So we already have most things what we need is:
(i could be using hardcoded stuff but I wanna generalize it for now, other files should be able to be uploaded like reports but this is for this feature in specific, and in the test part for now)

1. Directory upload in the frontend (upload a folder of slices, not just a single file)
2. Validation step before analysis - run parse_dicom_directory on the uploaded folder to verify:
- All files are valid DICOM
- Consistent SeriesInstanceUID (same series)
- Consistent modality/body part across slices
- Slice count makes sense (not just 1 file)
- Consistent image dimensions across slices
3. Then pass the validated directory path to monai.analyze_image for the 3D analysis


Create an api/upload-directory

to accept a list of upload files or a dir name

api/monai/validate series which will be kinda hardcoded to the test scenario now, because the objective is to be the AI doing this, but due to requests i just wanna test the inference workflow

It will also call the parse dicom directory which is already done since january, to check if each file is valid, this way we have validation
I updated the sample data endpoint, scanning subdirectories, and detecting SE* folders, which is again hardcoded, but for now...

All this and of course frontend changes

I have to update teh analyze_image from the monai server.py, to add a directory
The run inference and preprocess image were also altered in the mcp monai server




UPDATE

So recursively started analyzing the folder structure, checking for DICOM slides and files
I can upload a directory now, which sends all files and the backedn stores them all under dirname
So basiccally now, we can get the files from disk or upload, any file
I need to check the errors and verification of correct files to see if it works well


UPDATE, Im getting the canceled scope exception still, idk if it was because of the merge, ill look into it afterwards

After this Ill also test with some other dcm files but im going to wait to speak to you about it again

- During testing I forgot to correct this:
"Failed to analyze image: File /home/gabriel/Desktop/AgenticHealthMCP/mcp-monai/sample_data/Task09_Spleen/Task09_Spleen/imagesTr/._spleen_14.nii.gz is not a gzip file"
- Corrected.


It wasnt picking up modality on the DCM files
Maybe the metadata necessary doesnt come directly with the slices


TODO: Fix closing backend eerror



Got some errors running
```
Executing 'monai.run_inference' on 'monai' (transport: stdio) with args: {'image_path': '/home/gabriel/Desktop/AgenticHealthMCP/test_data/DICOM/ST000000/SE000002', 'model_name': 'spleen_ct_segmentation'}
Processing request of type CallToolRequest
Running inference on cuda...
Loading model from /home/gabriel/Desktop/AgenticHealthMCP/mcp-monai/bundles/spleen_ct_segmentation...
Preprocessing image...
Input tensor shape: torch.Size([1, 1, 154, 125, 116])
Running sliding window inference with ROI size [96, 96, 96]...
Output tensor shape: torch.Size([1, 2, 154, 125, 116])
Warning: FutureWarning: monai.transforms.spatial.array Orientation.__init__:labels: Current default value of argument `labels=(('L', 'R'), ('P', 'A'), ('I', 'S'))` was changed in version None from `labels=(('L', 'R'), ('P', 'A'), ('I', 'S'))` to `labels=None`. Default value changed to None meaning that the transform now uses the 'space' of a meta-tensor, if applicable, to determine appropriate axis labels.
Warning: UserWarning: Using a non-tuple sequence for multidimensional indexing is deprecated and will be changed in pytorch 2.9; use x[tuple(seq)] instead of x[seq]. In pytorch 2.9 this will be interpreted as tensor index, x[torch.tensor(seq)], which will result either in an error or a different result (Triggered internally at /pytorch/torch/csrc/autograd/python_variable_indexing.cpp:345.)
Warning: UserWarning: Using a non-tuple sequence for multidimensional indexing is deprecated and will be changed in pytorch 2.9; use x[tuple(seq)] instead of x[seq]. In pytorch 2.9 this will be interpreted as tensor index, x[torch.tensor(seq)], which will result either in an error or a different result (Triggered internally at /pytorch/torch/csrc/autograd/python_variable_indexing.cpp:345.)
```
Should be easy to solve


but before Im getting double unknown when running analyze image...
Okay I made a mistake, I had swapped, because before I had the monai analyze image which was detecting from intensity values through the heuristic, so if it hit a min of -500 it was a CT or MR if between 0 and 10000. 
Since we already have the modality from the dicom metadata validation, we should use tha tinstead of relying on the heurisitc
Therefore I also updated the detection, now its passing the metadata through

- Needed to update the detect modality from metadata to receive the is_dir

I tested the download model functionality and its working
[Download model functionality](../images/downloadmodel.png)

Although i got some errors, the sleect model disappeared, and im getting jsonrpc parsing errors
im also getting some warnings, im going to fix them
The refresh list was filtering by unknown due to previous tests, thats why they werent showing, from when I had teh unkown option when we had the manual selectors for modality and body part


Also im testing running the MRI for HEAD which is the DICOM file I have here, but the MONAI model that was **successfully downloaded** is designed for multi channel, and my MRI dataa is single channel, so the result might not be great

Inference failed: Given groups=1, weight of size [16, 4, 3, 3, 3], expected input[2, 1, 128, 128, 128] to have 4 channels, but got 1 channels instead
ACtually I got the exact error i thought i wouldnt okay...
I mean I guess this just proves the pipeline is working well, I just need better test data

it worked for the nii.gz file

I got the following results:
- Device
- Spleen detected
- Voxels and 0.18% volume, which is normal i guess since the spleen is a small organ, and relative to the abdomen its small
The ouput is a segmentation mask, that identifies the voxels in the 3D volume that belong to tje spleen
TODO: This would be more usefull if it could be analyzed, visalized as a 3D overlay on the CT scan, like 3D slicer.
TODO!!! (no idea how hard it is)
I should add more result metrics

So as an overview:

How does this help the doctors:
Overall it wont by itself, but in clinical practice

Segmetnation, what I ran, identifies where an organ is in a 3D scan and doctors use this for:
- surgical planning, knowing the organ boundaries size and position
- radiation therapy
- volume tracking
- pathology detection

whats missing that im planning to add 
- visualization
- measurments, the voxels should be converted to cm3 or volume
- report generation, which henrique is taking care of the templtes, we need templates for everything of course
- multi organ

Also I need to fix the warnings