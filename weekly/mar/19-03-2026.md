Gabriel


# Ideas

- Ways to solve the mounting directories problem:
Mount everything; Configurable mount root; Run watcher on the host and not docker; SFTP or NFS External directories mounted
The /home that was mounted was from the machine that belongs to the machine and nobodies directories, but my previous idea was to have the directories from each's computer, which was wrong, it should be from the machine, and now, should I allow for creation of folders? And paths?


Option 1 - The medic uploads files through the UI
They drag and drop files into the app, the app saves them to a folder on the server, and the workspace watches that folder. The medic never thinks about paths at all. This is basically what the upload feature already does - workspaces just add the "watch continuously" layer on top.

Option 2 - The data arrives on the server automatically
The DICOM scanner, PACS system, or hospital network already pushes files to a known folder on the server. IT configures this once. Medics don't set up workspaces - an admin does it once per department. Medic just sees the console and results. 

Think option 2 is the one we want

Ima wait for an answer to the question:
uma dúvida em relação ao que discutimos ontem: Os workspaces a ser monitorizados vão ser os da máquina onde o sistema está deployed, se quisermos monitorizar servidores externos podemos fazer isso por NFS. Mas quem é que define onde os ficheiros chegam e para que diretoria no servidor? é o scanner que envia automaticamente para uma diretoria? devemos conseguir adicionar diretorias novas no servidor ou só as que já existem podem ser monitorizadas?
Idk if it makes sense

# In progress

- At the moment taking care of the login part and users | user authentication  

# Done

- Renamed directories tab to workspaces
- Fixed linter errors
- Moved to debug mode: Report; History -> Sessions; Test
(Left references liek directories and history and just changed the names)