Gabriel


Added usage for logs


1. Fixed cellpose CPU Docker build downloading CUDA torch instead of CPU-only torch
2. Captured MONAI subprocess stderr so the actual crash reason shows in the error instead of just "Connection closed"
3. Added /api/system-stats backend endpoint (RAM, disk, CPU via psutil)                             
4. Added System Resources panel in Settings, visible in debug mode only, auto-refreshes every 30s