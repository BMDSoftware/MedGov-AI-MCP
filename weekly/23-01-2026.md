## Daily Report - 23-01-2026

Henrique

Tive a mexer na interface, fui basicamete buscar a que tinhas feito no outro projeto, adaptei um pouco mas falta coisas, como o worklow steps, q é bastante importante na minha opiniao.

Para alem disso, criei o workflow que usa apenas a imagem dada pelo utilizador e determina qual o modelo que deve usar.
Criei o agente que faz entao a selecao das tools e escolhe o modelo. Ele esta dividido em 3 ficheiros:

- agenticAgent.py -> Define o agente, as tools e o prompt
- gemini_client.py -> Cliente para comunicar com o Gemini
- tool_executor.py -> Executor das tools, basicamente o MCP client

Para testar:

```bash
cd frontend
npm install
npm run dev
```

```bash
cd orchestrator
uv run python backend.py
```

Estou a usar o uv, porque acho mais facil e porque a venv é chata de tar sempre a entrar e sair.

### Capacidades:

- Upload de imagens
- Agente pensa e usa o MCP
- Recebe resultados e mostra no frontend

### Problemas:

- Os resultados dão sempre que é Xray e não existe modelo, acho que como estou a mandar a imagem pro gemini, não está bem implementada
- Agente não está generalizado, seria bom termos um agente base e os outros agentes terem apenas a especialidade e tools diferentes

### Falta do que fiz:

- Workflow steps no frontend
- Melhorar o prompt do agente
- Tratar erros
- E secalhar em vez de agente direto, ja implementar uma arquitetura de orquestração de agentes
- FHIR tools (removi-o para nao estar a atrapalhar)

### Erros que cometi e pode ser útil:

alterar o mcp-config.json para o teu caminho

```json
"fhir": {
    "command": "python",
    "args": ["-m", "fhir_mcp_server"],
    "cwd": "/home/gabriel/Desktop/AgenticHealthMCP/fhir-mcp-server",
    "env": {
    "FHIR_BASE_URL": "http://localhost:8080/fhir"
    }
}
```