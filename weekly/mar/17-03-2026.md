Gabriel


# Keep testing

- Removed MCP server reappears after Docker restart (he said he'd fix later)
- Model doesn't realize it changed MCP tools after adding/removing

# Ideas

- Directory fetching could be corrected, but ima start with absolute paths and then maybe put it so we get like just Desktop ones, etc
- Persistent directories through db
- Directories watched globaly

- I could mount the /home directory in docker
(I need help with this because I can either, add /home or another global path; predefine directories; make a run script to run docker and the watcher at the same time)
Since with deployment it needs to be running Il hardcode the home path and mount it, and leave it as the other files to be changed

# In progress

- Testing directory workflow
- Changing frontend to look better

The problem with the paths for the directories is that docker can't see them, unless I add them as a volume, but I dont want to be adding as a volume, unless there's a way to do it on the fly.
I don't wanna use hardcoded paths

- Fixing adding a new directory and it being stopped
- Fixing the thinking logic and acceptance of custom prompt when dealing with a directory
- And the capacity to call tools


**The prompts should be specific for example I tried move from this directory but maybe it should be, move to this directory, and specify it**
Also Im fixing the ability of the AI to report back what it could or couldnt do (Console)


So, the prompt to move the file to another directory worked, but now its a little bit strange because, it first analyzed it, it moved it to uncertain within its directory, it moved it then to the other directory and then that other directory moved it to uncertain again... the workflow doesn't feel well
Should it know already the result? Should u be able to run different results in different directories?
And then also the problem that it's only trying to analyze and treat as NICIM or DICOM

Its taking the examples too literally and lacking creativity

I didn't want to have to nitpick the prompt for every case possible... but im starting to think thats the only chance
I wanted that if its an image, maybe it should run an inference on that image, if it feels like its on the wrong directory, it should change it, if its an incomplete report it should say its incomplete, if its already a analysis it should maybe complement it with a report, etc... These types of things, medical notes, the things spoken from the meeting from *11-03-2026.md*

I think Ill have to change the watcher

# Done

- Correctiong of docker 502 error (just a simple connectiong error)
- Correction of frontend errors, html entities weren't rendering 
- Icons changed (fontawesome - if you have any other cool fonts and frameworks or libraries, I'll focus more on the logic for now)
- Fixed watcher auto start
- Fixed custom prompts being passed
- Fixed not being able to add directories without custom prompt

# TODO

- For now directories are not tied to sesions, maybe fix? Maybe not and keep as another workflow
- Libraries for frontend and icons
- Define priority for directories (in case of name, take the one from database or take the name from the path)
- Make it able to open the directories?
- Diretory already added
- Different tools in different directories? Possibility to toggle on or off?