Gabriel

Okay sw what you did
First thing I saw you were using yaml instead of re, I don't know if you saw any advantage, I suppose you were using pyyaml or something like that.
I dont see the updated dependencies tho, or requirements.

- Corrected base exception error and fail gracefully from fhir
- I decided to give up on the local model for a while, I cannot make it better, the more tools we add the worse the model becomes, Ill either try a bigger model or have to change a lot fo things so ill just try and use the gemini API and this way I can make progress and see if its really working as we wnat 

We should fix this redundancy
- gemini_client._get_system_prompt() - the default prompt, used when no patient is selected and backend is Gemini
- ollama_client._get_system_prompt() - the default prompt, used when no patient is selected and backend is Ollama
- agenticAgent.set_patient_focus() - sets a custom prompt that overrides whichever client's default via self.llm_client.update_system_prompt()

So we're not eating up more requests after the rate limit is hit I created an exception and it stops after hitting the limit

Everything seems to be working much better, specially the context and cache.

Im losing requests too quickly, but it seems to be working fine now, we just need to make it more reliable, maybe better prompt and really make it work when running inferences

- Agent now explains failing logic for tool calling
From the logs:
the agent's text response should have been returned to the user immediately when a tool fails and the agent explains why.


TODO: Right now Im trying to take care of: database - upload picture or files - model (what do you wanna do with these) - analyze them (in background) and return to me the result once its finished (maybe in a results tab with the same context)

Im working on that workflow and also the .dcm analysis from 2D to 3D images and the different dicom formats you can have, raw .dcm or layered with headers jpged organized in directories

I couldnt setup payment because th payment setup is done in the Google AI Studio for some reason