[project]
name = "agentic_agent"
version = "0.1.0"
description = "AI agent for healthcare automation"
authors = [
]
readme = "README.md"
requires-python = ">=3.10, <3.12"

dependencies = [
    "fastapi",
    "dotenv",
    "requests",
    "google-genai",
    "python-multipart",
    "uvicorn",
    "torch", 
    "numpy",
    "mcp",
    "Pillow",
    "monai",
    "huggingface-hub",
    "torchvision",
    "nibabel",
    "pydicom",
    "itk",
    "beautifulsoup4",
]
