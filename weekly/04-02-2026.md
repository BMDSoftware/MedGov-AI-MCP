Gabriel

- When analyzing an image print the parameters body part and modality
- Fix download model (3. Called monai.download_model → Failed: Unknown model: CT)
- Show metadata extracted or download?
- Keep context

I saw your changes, I was a bit surprised because I don't remember us speaking about patient selection for now, not being a priority for now, rather get the whole workflow working with temporary cache for the model with the sole objective of analyzing one image with context from that session, and that's not working still, and adding patients is adding complexity to the system, because now what do we do, do we build a database with context or logs from the model that it reads each time you boot a patient, is it just a static database, we didn't speak about it, that requires planning first, and the way that it is now, is just as a placeholder, or were you just adding a place for the different parsed documents uploaded, because the parsing is called by the model and theres still some edges to refine.
I don't get why we're handling patients for now.
And why did you do a download images... you could've just put it on the .txt that I already had with the link or curl command or is it regarding the agent, or you're downloading the image you already gave the model, I don't get it, you give it the path locally, and the model fetches from there.



"Implementei agora uma nova funcionalidade que antes de começar a conversa com o agente, primeiro seleciona-se o paciente, e a conversa é personalizada para esse paciente.
Ainda não implementei contexto do paciente na conversa, mas já fica preparado para isso.
O que faz é entao, quando se seleciona o paciente, o backend atualiza o prompt do sistema do agente para saber que está a falar desse paciente."

I agree you should have context for patients, but not now, that implies way too many things, we need to get other stuff out of the way first, the use case was analyze image, and we are generalizing it, yes, so we can expand after, but not focusing on the other stuff right away, because when analyzing something, you might not even know what patient it is, it might be associated by what, ID? Name? Sometimes it should be anonymized, others it shouldn't, and again, why add that, what about the database, we don't even have the cache working for parsing a dicom file and using the info for choosing the image and the inference to call, and youre already calling a patient.
And honestly having to select a patient, could be useful with notes from the doctor and stuff, but as of right now, you will always be loading from zero the context, what do you even consider the context, the orchestrator is not taking any conclusions, so it's comments about the patient won't matter, also the analysis of the image will be kept, but then what, what do you use it for again?
I really think we shouldn't go this way now.



- Need to learn how the images work better and how we can inference them and analyze them, in 2D or 3D
- Need to take care of different DICOM files, some come with jpg and then the headers structure, others are just .dcm
- Reports, and analysis and inferece results (the results should be deterministic and not a subjective analysis by the agent)


- Agent skills for better moder performance and execution, but does it apply to the orchestrator??