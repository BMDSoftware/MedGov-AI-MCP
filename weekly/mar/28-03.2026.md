Henrique

Refactor of agenticagnet.py file. Now divide into several files, withtin agent directory. AgenticAgent.py stays but doesnt do anything, if any problems found, we can move the code back to agenticagent.py. 

Theres a readme in agent directory that explains the structure of the files.

To rollback, just change line 19 in backend.py to "from agent import AgenticAgent" and move the code back to "from agenticAgent import AgenticAgent"
