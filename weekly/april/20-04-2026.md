Gabriel



- Debugging
- Fixing errors and frontend


- Reading papers and documentation


1. MONAI server: logs RAM before sliding window + catches BaseException (not just Exception) so Python crashes show as error messages instead of killing the process
2. SSE: queued/running tasks no longer pre-populated in seen_ids on reconnect → task_queued toast will now show even after reconnects; also logs when SSE generator dies unexpectedly
3. Tool registry: detects dead session ("Connection closed") and auto-restarts the server, then retries the call - so the Test page keeps working after an inference crash



- Why is model back to running on CPU
- Error: The automated imaging analysis tool experienced an unexpected internal failure while attempting to process the scan, causing the task to abort before completion. Because the system could not successfully generate the required findings, you should manually interpret the scan and document your clinical assessment as you would for a standard study.
- SSE events and notification bug


RAM usage increased as well as CPU usage, I need to put GPU usage also in the settings, but it wasnt able to finish the ROI image count in 30m so it timedout, it should never take that long, but CPU was still in use


- Would make a general requirements be more optimal then using all these envs that can be redundant?
- After timeout RAM usage dropped and CPU usage
- Its failing locally as well when analyzing directories

"The artificial intelligence tool failed to process this scan because the internal configuration files do not match the expected format for this specific analysis model. Please perform your clinical evaluation manually and disregard any results generated by the system for this imaging study. You should notify the technical support team so they can address this software configuration issue."


- Fixed, went back on logging that was done, and now in a stable working version
- Was also failing because "swin_unetr_btcv_segmentation" wasn't receiving the right arguments as a MONAI model, maybe it can be disabled.


Still not working in deployment but working locally, could it be a deployment size limit? For the domain maybe?
When run inference is called its crashing


Maybe the problem was version missmatch between python 12 and python 11 in deployment
Took out the other model because it was choosing it unnecessarily way too often


/api/diagnostics endpoint