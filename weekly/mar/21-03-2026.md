Gabriel


Will have to change all database tables, beacuse login has to be associated with workspaces, sessions, analysis, settings, even the enabled or disabled tools
Login and registering tabs (instead of predefined users)
This multiple user logic will become a problem because the agent decision and current session id are tied directly, as well as tool state, etc
Basically it's using the same orchestrator

- Add auth layer
- Per user agent instances (instead of one global agent decision)
Watchers already run independently per workspace, so not a problem

- DB: users table, user_id on all other tables                          
- Backend: auth middleware, register/login endpoints, token validation, agent pool (dict of user_id -> AgenticAgent)                            
- Frontend: login/register pages, token stored in localStorage, sent in every request header


- Need to also make the mode, user based (if one user changes to debug mode, they all change)


Fixing errors with bcrypt

- Still getting errors with users and seeing each others stuff
- Background tasks had no userid

- React icons

- Be careful with CORS for deployment
- JWT Secret

- nginx was limiting size of files uploaded once again
(set to 10k max files) and no file size limit

No sessions are being created when reloading 
Db is getting locked

Not null session id

need to check for notifications regarding different users, does one get notified if other starts a process?


If an user is runnning an inference it will queue the others? For other users?

Results tab is only updating if I refresh the tab (fixed)


Lots of errors and considerations because of auth

The time it took in results is the refresh from when it was done

# TODO

- Login and authentication (Ongoing)

- Write more on the paper

- Fix pointer in frontend when pressing

- QA tests (would be useful since some changes we made along the way, stopped other stuff from working)

- Broken auth (Ongoing)

- Change buttons and frontend (Ongoing)

- Question: Same user shouldn't be able to add the same directory twice? (There might be some use cases where it's valid)

- Upgrade the way uploaded files and directories show in analysis mode

- I'd prefer a light mode

- Fix crashing when running severael inferences

- Add ability to cancel a running task, manually or the orchestrator?

- Distinguish what0s being uploaded file or directory in results tab


# Done

- Fixed analysis file upload in deployment
- Frontend changes (workspaces, main page, analysis page)
- Login and authentication

Workspaces borderglow and some effects
Sidebar navdock (i fell in love with react bits)
Analysis tab also changed
                        
For login created new files:
orchestrator/auth.py
frontend/src/auth.js
frontend/src/apiFetch.js
frontend/src/components/Login.jsx and .css

database.py modified, users table, user tool settings table, migrations for user_id, etc
backend.py register, login, me endpoints
Endpoints require "Depends(get_current_user)" so two u sers don't block each other
save and load disabled tools, meaning tool state is per user
authentication in frnotend

config.js is fixed
jwt token stateless

Fixed auth issues
Results werent popping up in tab after analysis

Home page changes

Fixed errors with bcrypt

SSE Handler passing current user and user id

Reset chat state on user logout

Fixed cursor and ability to select text

Workspace icons and frontend changes

Fixed running inferences on files and directories

Distinguish in results from file and directory

Added a semaphore so the inferences run one at a time, since running several inferences at once might crash, so it's better for them to be queued

Consequently created tag "queued" instead of failed; running; done

Resolve model name added to monai server (so it doesn't call by name and risks failing, but discovers the corresponding model better)

Notifications fixed
Result tab updating only on refresh fixed

Fixed SSE EVENTS