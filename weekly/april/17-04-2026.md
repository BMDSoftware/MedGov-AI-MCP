Gabriel


- More debugging
When running the inference manually, there's no stderr capture from the MONAI subprocess, when it OOM out of memory, it dies, it gets connection closed with no details

- Capture MONAI subprocess stderr so I can see the actual crash reason
- Check available memory before running inference and warn if its likely to OOM
- Find a way to not occupy as much memory
- Also the ROI image seems to hang still when running


- Optimize cache for docker compose