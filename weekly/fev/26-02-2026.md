Gabriel


Still not explaining the error
"The AI had a problem processing this specific scan and wasn't able to give you any results. Please review the image manually, as the AI analysis failed."

Im going to unwrap the discussion message
- Graceful shutdown still not working


Is running same model, although it lists different ones, it runs the last one being ran by list models.
Fixed by only queueing what the LLM asks for
Unread tasks counts for the notifications



Debug mode (current behavior):
- Every non-built-in tool pauses and asks for confirmation: "I want to execute: monai.analyze_image with params {...}"
- AI responses can include raw JSON and technical details
- Good for development and testing


(Had to create a different prompt for this)
Normal mode (new):
- Tools execute automatically — no confirmation prompts
- A short communication style block is appended to the system prompt, telling the AI:
- Respond in formal clinical language
- Never mention tool names, JSON, file paths, internal system details
- Say things like "I have submitted the analysis. Progress is visible in the Results tab."
- Keep responses to 2, 4 sentences
- The user just sees professional replies like a radiology assistant would give


## Taken from yesterday's discussion


Notes:
- Every file and directory are uploaded, it isnt just the path they are written into DB.uploads_dir, on the same machine because i dont have a cloud server 

Done:
- 3 error types were defined for the AI to return in the results tab
- LLM adds explanation to the error with _explain_inference_function, its raised when MCP returns an error
(It's basically a hardcoded skill)
- updated results.jsx
- Added to results tab, body part, modality, model run, file or directory (it might not have those but were mainly dealing with inferences)
- results tab notification and pop up notification correction
- commented frontend patients tab
- created a homepage
- debug mode and normal mode (one with the workflows and tool execution confirmation, one just for medics)

TODO:
- add hints for example asking for the body part and modality (already done - test)
- add hints look for them in folder and path name
- optimizations