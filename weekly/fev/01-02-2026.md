Gabriel

Plans for today:

- pydicom
- pasing
- dataset for dicom generation images

Im not going to be focusing on the report for now
I also asked gemini to aggregate all the things Ive found regarding Pydicom library in python and the structure of these files and wrote a doc for it, so check it, because it will help building the parser, and in your case to understand the structure.


The first use case will be just using the parser to get the modalities and body parts in order to send them to the orchestrator so that when asking to analyze the image the right inference model is called in MONAI. (We would later expand this because there's more useful metadata that can be used for example to write the reports afterwards)
Im not harcoding into the parser, im making the parser plain and simple, and with the parameters returned the orchestrator will choose the model

Looking into datasets I noticed some of them might have certain fields missing, so the parser is extracting only the available tags, and will show which fields are or not found

I added the parser to the local tools of the orchestrator




!!!!
TODO: We should look into: if its justifiable having gemini_client.py and ollama_client.py and if we cant aggregate those into the same file or have just a folder with contextes, etc


The dicom_parser.py:
- extracts all tags from a DICOM file
- parses all DICOM in a folder gruoped by series
- loads a pixel data as numpy array

The agenticAgent.py (tools added)
- parses a single DICOM file
- parses a directory of DICOMS

I had the local.tool which was being checked before executiona nd routing to a local tool execution instead of the MCP registry
I changed it by adding mcp-utils to have these custom tools and then we have all in MCP registry, idk if it makes sense