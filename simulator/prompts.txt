ROI: New pathology images have arrived for cell counting triage. For each image, run cell segmentation and count the cells. If the count is below 1000, move the image to /app/workspaces/CELLPOSE/normal/. If the count is 1000 or above, move the image to /app/workspaces/CELLPOSE/urgent/. After processing all images, write a triage log in the current workspace directory listing each filename, its exact cell count, and whether it was routed as URGENT or NORMAL. Format as a markdown list.

CELLPOSE: New images have arrived. Check which subfolder each image is in. Images in the "urgent" subfolder have high cell density and require clinical attention. For each urgent image, treat it as a separate anonymous patient with no known identity. First create an anonymous Patient resource on the FHIR server using only the image filename as an internal identifier - no name, no demographics. Then create one Observation linked to that Patient recording the high cell density finding and recommending immediate clinical review. Do this individually for each urgent image. Images in the "normal" subfolder require no action.
