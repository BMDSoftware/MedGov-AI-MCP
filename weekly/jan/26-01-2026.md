Henrique

Adicionei apenas nas settings o enabling and disabling de tools e um endpoint para refresh da config

Implementei tanto pro gemini como pro ollama mas nao testei o ollama (mas penso que deve funcionar)

A alteração de MCP é feita através da alteraçao do ficheiro mcp-config.json e depois a chamada ao endpoint /api/refresh-config para recarregar a config, q dps atualiza as tools do agente

-----

Fiz alterações nas tools, onde agora é tudo feito através do mcp client, e o mcp client é inicializado com a config do ficheiro mcp-config.json.

Agora está tudo no tool_registry.py, desde a leitura do ficheiro de config, inicialização do mcp client, descoberta das tools, e execução das tools.

Tambem mexi em como é lido os mcps http, agora
```json
    "fhir": {
      "type": "http",
      "url": "http://localhost:8000/mcp",
      "transport": "http"
    }
```
fui buscar como tava no github do fhir e só adicionei o "transport": "http", para distinguir na criação do MCPClient


Por ultimo mexi nos prints do mcp-monai para logs, que estavam a dar erro por enviarem para o stdout, e o agente nao conseguia lidar com isso.

### Problemas conhecidos / To do

- Se o mcps http nao estiverem a correr, dá erro e não consegui lidar com a excesão pelo que o agente crasha.

