Gabriel



- The hang was still happening: so I suppose it's because the model is trying to see the image, with the byte data that it's sent, and that happens because of the different model choice, when sending .dcm it doesn't hang, but with .png or .jpeg it does, so I changed it to send only the path, and will test now.