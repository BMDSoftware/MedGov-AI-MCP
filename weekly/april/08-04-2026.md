Gabriel



- Need to deploy to test, we need to get another api
- Max number of iterations switched to 10
- Problem might be cuz machine doent have GPU or the build fails so it falls back on old images
- Deploy is taking 14 seconds
- Builds are passing
- Overwhelming logs
- Fixing ruff
- GPU hanging cuz process is crashing because its going over the processing limit?


Okay fixed the hang, in GPU and CPU, fixed mask, now its processing cellpose well
1. save_overlay was calling cellpose.utils.outlines_list() - a per-cell Python loop that traces every boundary pixel for thousands of cells. Inference finished (GPU deloaded), but the task kept running because the overlay was grinding through ~3000 cells on CPU. Fixed by switching to utils.masks_to_outlines() (built-in, O(pixels) not O(cells)) + a 60s asyncio.wait_for as a safety net.
2. For actual deadlocks (process alive but 0% CPU - OpenMP threading issue), added a general _mcp_subprocess_watchdog that monitors CPU and fails the task instead of hanging forever.    
       


# Done

- Added a debug panel for the deployment - I dont know why the analysis mode wasnt answering nor why MCP servers weren't being detected, needs fixing
- api/logs
- Adding startup logs to check why MCP servers arent showing up
- Reloading tools fix
- MCP config docker was overwritten with a change.... pretty sure that was the problem
- Timeout added for model not to hang
- Increased times for refreshing and pooling
- GPU hang fix
- Watchdog for subprocesses
- Also fixed segmentation mask and normal mask


# Problems

- Reload config makes builtin disappear
- so the process doesn't crash, it just hangs after GPU deloads. The pipe stays open (process still alive), so EOF is never triggered. We need a watchdog that detects the subprocess is idle (0% CPU, not making progress) and fails the task.


# TODO

- Maybe a bail out early for malformed tool calls
- If the model cant answer because it doesnt have tools for that it should say that?
- Add tests to stop breaks like this from happening



- Options for the GPU hanging (it's running out of memory)
 
We could:
- Make a shared session, reuse the same process
- Force CPU tasks for subprocesses
- Reduce main session VRAM, meaning, not preloading the cpsam model, this would make the first call slower
- Close and reopen process according to calls
(Also, what if we disable the tool, since it's preloaded should it be unloaded, and should we activate the load when we activate the tool?)
Might run out of VRAM regardless