Gabriel



Nao consegui fazer mais cedo por causa do temporal

Okay so first I was understanding the skills you implemented, and I understood that instead of dumping all the domain knowledge into the system prompt you gave the LLM a directory of available skills, adn the LLM only loads the full instructions when needed.
The flow is: 
- llm sees a list of skill names +  short descriptions in the system prompt
- when a task requires a skill you calling skills.read_skill to load the full skill.md
- if its needed more detail it calls read references
- then it executes teh actual work using skills execute script or the existing mcp tools
and in this last point is where my doubt resides because shouldnt the skills should be used to call the MCP tools or not? Instead of having two seperate actions the same way that I made the custom MCP utils for the dicom parser instead of putting it locally in the agent.
Isn't it redundant this way?

And try to document more the things please because it's easier for me to understand them better.


The skill.md is from calude scientific skills repo, and its a generic pydicom documentation, how to read dicom files for example, although we already had the parser but all fine we can have both approaches and see whats best
common_tags is the tags from the dicom
Did you test the MCP utils one? Did you compare to see which was working better
What problems are the skills resolving right now.
Did you compare to the MCP utils? What was the result you got. 
Again you did the extract_metadata.py which was already done and working with MCP utils
And you did the anonymize dicom .py also which okay mightve been for test, but we're leaving the focus right now, why didn't you do the analyze the image one?

You have the MCP-skills ok good

- Maybe we should remove the set_patient_focus for now no?

And arent you being redudndant loading the skills?
Because youre loading skills via "load_all_skills()" and youre using the SkillsManager which is loading them independently

Also for a single skill, pydicom, the agent would read skill file, read references maybe, execute the script.
Thats 3 to 4 toolcalls just to extract the dicom metadata, when you had utils.parse_dicom MCP that does it in 1 call.
This makes sense when you have a lot of skills and can't fit everything in context tho, so we will have to explain this to the teachers, the scalling potential.

Right now i think the agent will still prefer to be using the MCP tool call even tho we have the skills.
Skills will be relevant if the agent doesnt have a direct MCP tool for the task and the agent is specifiically instructed in the system prompt to use skills first.

- Build database
- Build context and cache
- Async workflows
- Agent not calling the right tools
    - Bad tool descriptions
    - Bad system prompt
    - Too many tools?
    - model is small
- persistant logs for the model
- Context memory problem


### Implemented

So I added SessionContext with record(), build context string(), clear() adn set_patient()
Added extract_key_data method that extracts facts from each tool type (DICOM Metadata, monai models, fhir resources, radlex)
Session context recorded after successful tool execution in both execute_task() and confir_tool_execution()
Added reset_session context
Wrise setp patient into set patient focus
I also added endpoits for reset session and clear files